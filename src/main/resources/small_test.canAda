with Ada.Text_IO; use Ada.Text_IO;
procedure unDebut is
choix : integer ;
a : integer ;
b : bk ;

type point is record
x : integer ;
y : character ;
end record;

c : point ;

function aireRectangle(larg : in integer; long : in out integer) return point is
aire: point;
giga: character;
begin
return aire ;
end aireRectangle ;


begin
c.x := a + aireRectangle(3, 4).y ;
end;