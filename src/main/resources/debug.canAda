with Ada.Text_IO; use Ada.Text_IO;
procedure unDebut is


type point is record
x, y : integer;
end record;

function distance (p1 : point; x : integer) return integer is
begin
return x;
end distance;

choix : point;

    -- PROCEDURE PRINCIPALE
begin
choix.x := 5;
end unDebut;
