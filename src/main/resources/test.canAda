with Ada.Text_IO; use Ada.Text_IO;
procedure unDebut is
function aireRectangle(larg : in integer; long : in out integer) return integer is
aire: integer;
giga: character;
begin
    long := larg * long ;
    larg := 3 ;
    aire2 := 'c' ;
    aire := 'c' ;
    giga := 2 ;
return aire ;
end aireRectangle ;

function returnChar(larg : integer; long : integer) return character is
aire: integer;
giga: character;
begin
    aire2 := larg * long ;
    giga := aireRectangle(2, 3) ;
    aire := giga ;
return aire ;
end returnChar ;

function perimetreRectangle(larg : character; long : integer) return integer is
p : integer;
begin
    p := larg*2*4 + long*2 ;
return p ;
end perimetreRectangle;

    -- VARIABLES
choix : integer ;
    -- PROCEDURE PRINCIPALE
begin
    choix := 2;
    if choix = 1
        then valeur := perimetreRectangle(aireRectangle(returnChar('c', 7), 4), 3) ;
            put(valeur) ;
        elsif choix = 2
            then valeur := aireRectangale(2, 3) ;
            put(valeur) ;
        else valeur := aireRectangale(2, 3) ;
            put(valeur) ;
    end if;
end unDebut ;