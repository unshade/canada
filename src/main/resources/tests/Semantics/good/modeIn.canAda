with Ada.Text_IO; use Ada.Text_IO;
procedure TestIn is
    X : integer;
    function Get(X : in integer) return integer is
    begin
        put(X); -- I can read X
        return X;
    end Get;
begin
   put(X); -- I can read X
end TestIn;
